{
  "cassetteVersion": 1,
  "createdAt": "2026-07-10T06:36:51Z",
  "id": "0da52c197c0067ecd5e9240b13affc4788a5f0c9522a5a8716085086ad6d57c9",
  "model": "claude-cli:sonnet",
  "prompt": "# Render request (prompt timesheet-v1)\nModule: timestore\nStack: python-lib\n\n## Definitions\n- Entry: a plain dict with keys `id`, `project`, `date`, `hours`, `status`.\n- Store file: a JSON file holding every entry; its path comes from an explicit constructor argument or the `TIMESHEET_STORE` environment variable.\n- ISO week: a week label written `YYYY-Www` with a zero-padded week number, for example `2026-W28`.\n- StoreError: the typed error for store misuse \u2014 a missing store path, an invalid date, an invalid week label, or an unknown entry id.\n\n## Implementation requirements\n- Use Python 3.12 and only the standard library.\n- Expose `TimeStore` and `StoreError` from `src/timestore/`.\n- `TimeStore(path=None)` uses the explicit path when given; otherwise it reads the `TIMESHEET_STORE` environment variable at construction time. With neither set, the constructor raises `StoreError` immediately.\n- A missing store file means an empty store; the file and its parent directories are created on the first write.\n- `add_entry(project, date, hours)` assigns the next integer id starting at 1, stores the entry with status `draft`, persists it, and returns the stored entry.\n- Expose `get_entry(entry_id)`, `update_entry(entry_id, **fields)`, `delete_entry(entry_id)`, and `list_entries()`; `update_entry` merges the given fields into the stored entry, persists, and returns the updated entry.\n- `entries_for_project(project)` returns the entries whose `project` equals the given name, in insertion order.\n- `entries_for_date(date)` returns the entries whose `date` equals the given date string, in insertion order.\n- `entries_for_week(week)` returns the entries whose `date` falls in the given ISO week, using the ISO 8601 calendar for year boundaries.\n- Dates are strings `YYYY-MM-DD`; `add_entry`, `update_entry`, and `entries_for_date` raise `StoreError` at call time for a value that is not a valid calendar date in that format.\n- `get_entry`, `update_entry`, and `delete_entry` raise `StoreError` at call time when no entry has the given id.\n- `entries_for_week` raises `StoreError` at call time for a week label not in the `YYYY-Www` form.\n- Unit tests use pytest.\n\n## Test requirements\n- Conformance tests use pytest.\n- Conformance tests call only the public API and point every store at a `tmp_path` file.\n- Include round-trip persistence, unknown-id errors, invalid dates, and an ISO year-boundary week query.\n\n## Stack adapter guidance\n- Write the implementation under src/ and generated unit tests under tests/ named test_*.py (pytest discovery); every functional unit must ship at least one unit test.\n- Write the conformance test for the CURRENT unit only, with root 'conformance', at the path FRn/... (for example FR1/test_fr1.py). Do not add a tests/ or module-name prefix, and do not create or modify earlier units' conformance tests.\n- Do not write outside the generated module patch root or conformance patch root.\n- Grow the public API incrementally for the current unit and prior rendered units; do not create placeholder stubs for future functional units.\n- Declare third-party dependencies in the generated pyproject.toml [project] dependencies; Mint installs them before tests run.\n\n## Current module files (already rendered for earlier units)\nExtend and stay consistent with this code; reuse its exact public names.\n#### src/timestore/__init__.py\n```python\n\"\"\"Timesheet entry store backed by a JSON file.\"\"\"\n\nfrom __future__ import annotations\n\nimport json\nimport os\nfrom datetime import date as _date\nfrom pathlib import Path\nfrom typing import Any\n\n\nclass StoreError(Exception):\n    \"\"\"Raised for store misuse: missing path, invalid date/week, or unknown entry id.\"\"\"\n\n\nclass TimeStore:\n    def __init__(self, path: str | os.PathLike[str] | None = None) -> None:\n        if path is None:\n            path = os.environ.get(\"TIMESHEET_STORE\")\n        if not path:\n            raise StoreError(\"no store path given: pass path= or set TIMESHEET_STORE\")\n        self._path = Path(path)\n        self._entries: dict[int, dict[str, Any]] = {}\n        self._load()\n\n    def _load(self) -> None:\n        if not self._path.exists():\n            self._entries = {}\n            return\n        with self._path.open(\"r\", encoding=\"utf-8\") as f:\n            raw = json.load(f)\n        self._entries = {entry[\"id\"]: entry for entry in raw}\n\n    def _save(self) -> None:\n        self._path.parent.mkdir(parents=True, exist_ok=True)\n        with self._path.open(\"w\", encoding=\"utf-8\") as f:\n            json.dump(list(self._entries.values()), f, indent=2)\n\n    @staticmethod\n    def _validate_date(value: str) -> None:\n        try:\n            _date.fromisoformat(value)\n        except (ValueError, TypeError) as exc:\n            raise StoreError(f\"invalid date: {value!r}\") from exc\n\n    def add_entry(self, project: str, date: str, hours: float) -> dict[str, Any]:\n        self._validate_date(date)\n        next_id = max(self._entries, default=0) + 1\n        entry = {\n            \"id\": next_id,\n            \"project\": project,\n            \"date\": date,\n            \"hours\": hours,\n            \"status\": \"draft\",\n        }\n        self._entries[next_id] = entry\n        self._save()\n        return dict(entry)\n\n    def get_entry(self, entry_id: int) -> dict[str, Any]:\n        try:\n            return dict(self._entries[entry_id])\n        except KeyError as exc:\n            raise StoreError(f\"unknown entry id: {entry_id}\") from exc\n\n    def update_entry(self, entry_id: int, **fields: Any) -> dict[str, Any]:\n        if entry_id not in self._entries:\n            raise StoreError(f\"unknown entry id: {entry_id}\")\n        if \"date\" in fields:\n            self._validate_date(fields[\"date\"])\n        entry = self._entries[entry_id]\n        entry.update(fields)\n        self._save()\n        return dict(entry)\n\n    def delete_entry(self, entry_id: int) -> None:\n        try:\n            del self._entries[entry_id]\n        except KeyError as exc:\n            raise StoreError(f\"unknown entry id: {entry_id}\") from exc\n        self._save()\n\n```\n\n## Units already rendered\n- FR1: Create, persist, and read entries\n- FR2: Update, delete, and unknown-id errors\n- FR3: Query by project, by date, and by ISO week\n\n## Implement unit FR3: Query by project, by date, and by ISO week\nSpec:\n- `entries_for_project(project)` returns only the entries whose `project` equals the given name.\n- `entries_for_date(date)` returns only the entries whose `date` equals the given date string; an invalid date raises `StoreError` at call time.\n- `entries_for_week(week)` returns the entries whose `date` falls in the ISO week labelled `YYYY-Www`; ISO year boundaries follow the ISO 8601 calendar.\n- A week label not in the `YYYY-Www` form raises `StoreError` at call time.\nAcceptance:\n- With entries stored for projects `Apollo` and `Zephyr`, `entries_for_project(\"Apollo\")` returns only the `Apollo` entries.\n- With entries dated `2026-07-06` and `2026-07-07`, `entries_for_date(\"2026-07-06\")` returns only the entries dated `2026-07-06`.\n- `entries_for_date(\"2026-13-40\")` raises `StoreError` whose message contains `invalid date`.\n- With an entry dated `2026-07-06`, `entries_for_week(\"2026-W28\")` returns that entry and `entries_for_week(\"2026-W27\")` returns an empty list.\n- An entry dated `2027-01-01` is returned by `entries_for_week(\"2026-W53\")`, because that date belongs to ISO year 2026.\n- `entries_for_week(\"2026-28\")` raises `StoreError` whose message contains `invalid week`.\n\nPhase: unit (attempt 1)\n\nReturn the JSON patch now.",
  "promptVersion": "timesheet-v1",
  "request": {
    "attempt": 1,
    "module": "timestore",
    "phase": "unit",
    "promptHash": "5e7394d1499c22a4b51b9bfa77ad5764da02cc0c3b66860b8f38e729c3ad1a54",
    "unit": "FR3"
  },
  "response": "{\"summary\": \"Add entries_for_project, entries_for_date, entries_for_week query methods to TimeStore\", \"files\": [{\"path\": \"src/timestore/__init__.py\", \"contents\": \"\\\"\\\"\\\"Timesheet entry store backed by a JSON file.\\\"\\\"\\\"\\n\\nfrom __future__ import annotations\\n\\nimport json\\nimport os\\nimport re\\nfrom datetime import date as _date\\nfrom pathlib import Path\\nfrom typing import Any\\n\\n\\nclass StoreError(Exception):\\n    \\\"\\\"\\\"Raised for store misuse: missing path, invalid date/week, or unknown entry id.\\\"\\\"\\\"\\n\\n\\n_WEEK_RE = re.compile(r\\\"^(\\\\d{4})-W(\\\\d{2})$\\\")\\n\\n\\nclass TimeStore:\\n    def __init__(self, path: str | os.PathLike[str] | None = None) -> None:\\n        if path is None:\\n            path = os.environ.get(\\\"TIMESHEET_STORE\\\")\\n        if not path:\\n            raise StoreError(\\\"no store path given: pass path= or set TIMESHEET_STORE\\\")\\n        self._path = Path(path)\\n        self._entries: dict[int, dict[str, Any]] = {}\\n        self._load()\\n\\n    def _load(self) -> None:\\n        if not self._path.exists():\\n            self._entries = {}\\n            return\\n        with self._path.open(\\\"r\\\", encoding=\\\"utf-8\\\") as f:\\n            raw = json.load(f)\\n        self._entries = {entry[\\\"id\\\"]: entry for entry in raw}\\n\\n    def _save(self) -> None:\\n        self._path.parent.mkdir(parents=True, exist_ok=True)\\n        with self._path.open(\\\"w\\\", encoding=\\\"utf-8\\\") as f:\\n            json.dump(list(self._entries.values()), f, indent=2)\\n\\n    @staticmethod\\n    def _validate_date(value: str) -> None:\\n        try:\\n            _date.fromisoformat(value)\\n        except (ValueError, TypeError) as exc:\\n            raise StoreError(f\\\"invalid date: {value!r}\\\") from exc\\n\\n    def add_entry(self, project: str, date: str, hours: float) -> dict[str, Any]:\\n        self._validate_date(date)\\n        next_id = max(self._entries, default=0) + 1\\n        entry = {\\n            \\\"id\\\": next_id,\\n            \\\"project\\\": project,\\n            \\\"date\\\": date,\\n            \\\"hours\\\": hours,\\n            \\\"status\\\": \\\"draft\\\",\\n        }\\n        self._entries[next_id] = entry\\n        self._save()\\n        return dict(entry)\\n\\n    def get_entry(self, entry_id: int) -> dict[str, Any]:\\n        try:\\n            return dict(self._entries[entry_id])\\n        except KeyError as exc:\\n            raise StoreError(f\\\"unknown entry id: {entry_id}\\\") from exc\\n\\n    def update_entry(self, entry_id: int, **fields: Any) -> dict[str, Any]:\\n        if entry_id not in self._entries:\\n            raise StoreError(f\\\"unknown entry id: {entry_id}\\\")\\n        if \\\"date\\\" in fields:\\n            self._validate_date(fields[\\\"date\\\"])\\n        entry = self._entries[entry_id]\\n        entry.update(fields)\\n        self._save()\\n        return dict(entry)\\n\\n    def delete_entry(self, entry_id: int) -> None:\\n        try:\\n            del self._entries[entry_id]\\n        except KeyError as exc:\\n            raise StoreError(f\\\"unknown entry id: {entry_id}\\\") from exc\\n        self._save()\\n\\n    def list_entries(self) -> list[dict[str, Any]]:\\n        return [dict(entry) for entry in self._entries.values()]\\n\\n    def entries_for_project(self, project: str) -> list[dict[str, Any]]:\\n        return [dict(e) for e in self._entries.values() if e[\\\"project\\\"] == project]\\n\\n    def entries_for_date(self, date: str) -> list[dict[str, Any]]:\\n        self._validate_date(date)\\n        return [dict(e) for e in self._entries.values() if e[\\\"date\\\"] == date]\\n\\n    def entries_for_week(self, week: str) -> list[dict[str, Any]]:\\n        match = _WEEK_RE.match(week)\\n        if not match:\\n            raise StoreError(f\\\"invalid week: {week!r}\\\")\\n        target_year, target_week = int(match.group(1)), int(match.group(2))\\n        result = []\\n        for entry in self._entries.values():\\n            entry_date = _date.fromisoformat(entry[\\\"date\\\"])\\n            iso_year, iso_week, _ = entry_date.isocalendar()\\n            if iso_year == target_year and iso_week == target_week:\\n                result.append(dict(entry))\\n        return result\\n\", \"action\": \"write\", \"root\": \"module\"}, {\"path\": \"tests/test_fr3.py\", \"contents\": \"from __future__ import annotations\\n\\nimport pytest\\n\\nfrom timestore import StoreError, TimeStore\\n\\n\\ndef _store(tmp_path):\\n    return TimeStore(path=tmp_path / \\\"store.json\\\")\\n\\n\\ndef test_entries_for_project_filters(tmp_path):\\n    store = _store(tmp_path)\\n    store.add_entry(\\\"Apollo\\\", \\\"2026-07-06\\\", 4)\\n    store.add_entry(\\\"Zephyr\\\", \\\"2026-07-06\\\", 2)\\n    store.add_entry(\\\"Apollo\\\", \\\"2026-07-07\\\", 3)\\n\\n    result = store.entries_for_project(\\\"Apollo\\\")\\n\\n    assert [e[\\\"project\\\"] for e in result] == [\\\"Apollo\\\", \\\"Apollo\\\"]\\n\\n\\ndef test_entries_for_date_filters(tmp_path):\\n    store = _store(tmp_path)\\n    store.add_entry(\\\"Apollo\\\", \\\"2026-07-06\\\", 4)\\n    store.add_entry(\\\"Apollo\\\", \\\"2026-07-07\\\", 3)\\n\\n    result = store.entries_for_date(\\\"2026-07-06\\\")\\n\\n    assert [e[\\\"date\\\"] for e in result] == [\\\"2026-07-06\\\"]\\n\\n\\ndef test_entries_for_date_invalid_raises(tmp_path):\\n    store = _store(tmp_path)\\n\\n    with pytest.raises(StoreError, match=\\\"invalid date\\\"):\\n        store.entries_for_date(\\\"2026-13-40\\\")\\n\\n\\ndef test_entries_for_week_matches_and_excludes(tmp_path):\\n    store = _store(tmp_path)\\n    store.add_entry(\\\"Apollo\\\", \\\"2026-07-06\\\", 4)\\n\\n    assert len(store.entries_for_week(\\\"2026-W28\\\")) == 1\\n    assert store.entries_for_week(\\\"2026-W27\\\") == []\\n\\n\\ndef test_entries_for_week_iso_year_boundary(tmp_path):\\n    store = _store(tmp_path)\\n    store.add_entry(\\\"Apollo\\\", \\\"2027-01-01\\\", 4)\\n\\n    result = store.entries_for_week(\\\"2026-W53\\\")\\n\\n    assert len(result) == 1\\n    assert result[0][\\\"date\\\"] == \\\"2027-01-01\\\"\\n\\n\\ndef test_entries_for_week_invalid_label_raises(tmp_path):\\n    store = _store(tmp_path)\\n\\n    with pytest.raises(StoreError, match=\\\"invalid week\\\"):\\n        store.entries_for_week(\\\"2026-28\\\")\\n\", \"action\": \"write\", \"root\": \"module\"}, {\"path\": \"FR3/test_fr3.py\", \"contents\": \"from __future__ import annotations\\n\\nimport pytest\\n\\nfrom timestore import StoreError, TimeStore\\n\\n\\ndef test_entries_for_project(tmp_path):\\n    store = TimeStore(path=tmp_path / \\\"store.json\\\")\\n    store.add_entry(\\\"Apollo\\\", \\\"2026-07-06\\\", 4)\\n    store.add_entry(\\\"Zephyr\\\", \\\"2026-07-06\\\", 2)\\n\\n    result = store.entries_for_project(\\\"Apollo\\\")\\n\\n    assert len(result) == 1\\n    assert result[0][\\\"project\\\"] == \\\"Apollo\\\"\\n\\n\\ndef test_entries_for_date(tmp_path):\\n    store = TimeStore(path=tmp_path / \\\"store.json\\\")\\n    store.add_entry(\\\"Apollo\\\", \\\"2026-07-06\\\", 4)\\n    store.add_entry(\\\"Apollo\\\", \\\"2026-07-07\\\", 3)\\n\\n    result = store.entries_for_date(\\\"2026-07-06\\\")\\n\\n    assert len(result) == 1\\n    assert result[0][\\\"date\\\"] == \\\"2026-07-06\\\"\\n\\n\\ndef test_entries_for_date_invalid_date_raises(tmp_path):\\n    store = TimeStore(path=tmp_path / \\\"store.json\\\")\\n\\n    with pytest.raises(StoreError, match=\\\"invalid date\\\"):\\n        store.entries_for_date(\\\"2026-13-40\\\")\\n\\n\\ndef test_entries_for_week_basic(tmp_path):\\n    store = TimeStore(path=tmp_path / \\\"store.json\\\")\\n    store.add_entry(\\\"Apollo\\\", \\\"2026-07-06\\\", 4)\\n\\n    assert len(store.entries_for_week(\\\"2026-W28\\\")) == 1\\n    assert store.entries_for_week(\\\"2026-W27\\\") == []\\n\\n\\ndef test_entries_for_week_iso_year_boundary(tmp_path):\\n    store = TimeStore(path=tmp_path / \\\"store.json\\\")\\n    store.add_entry(\\\"Apollo\\\", \\\"2027-01-01\\\", 4)\\n\\n    result = store.entries_for_week(\\\"2026-W53\\\")\\n\\n    assert len(result) == 1\\n    assert result[0][\\\"date\\\"] == \\\"2027-01-01\\\"\\n\\n\\ndef test_entries_for_week_invalid_label_raises(tmp_path):\\n    store = TimeStore(path=tmp_path / \\\"store.json\\\")\\n\\n    with pytest.raises(StoreError, match=\\\"invalid week\\\"):\\n        store.entries_for_week(\\\"2026-28\\\")\\n\", \"action\": \"write\", \"root\": \"conformance\"}]}",
  "system": "You are a code renderer in a regenerative coding system. You implement exactly one functional unit. Respond with ONLY a single JSON object matching this schema:\n{\"summary\": \"<short>\", \"files\": [{\"path\": \"<rel path>\", \"action\": \"write|delete\", \"contents\": \"<file text, for write>\", \"root\": \"module|conformance\"}]}\nDo not include prose, markdown fences, or explanations outside the JSON."
}
