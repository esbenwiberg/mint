{
  "cassetteVersion": 1,
  "createdAt": "2026-07-08T18:53:23Z",
  "id": "5b8f40a7028991fca9279af9a90997fad92b8c1497ed7bebaca772442883ef9c",
  "model": "claude-cli:sonnet",
  "prompt": "# Render request (prompt timesheet-v1)\nModule: rules\nStack: python-lib\n\n## Definitions\n- Status: one of `draft`, `submitted`, `approved`, `rejected`.\n- Day total: the sum of hours a person already has registered on one calendar date.\n- RuleError: the typed error raised for an invalid value or an illegal status transition.\n\n## Implementation requirements\n- Use Python 3.12 with pure functions only \u2014 no I/O, no module state, no third-party dependencies.\n- Expose `RuleError`, `validate_hours(hours, day_total=0.0)`, `transition(current, target)`, and `assert_editable(status)` from `src/rules/`.\n- Every rule check raises `RuleError` at call time; nothing is validated at import time.\n- `validate_hours` returns None for positive hours whose new day total stays within the 24-hour day; it raises `RuleError` for zero or negative hours, or when the new day total would exceed 24.\n- `transition` returns the target status for exactly three legal moves \u2014 `draft` to `submitted`, `submitted` to `approved`, `submitted` to `rejected` \u2014 and raises `RuleError` for every other pair, including unknown status names.\n- `assert_editable` returns None for `draft`, `submitted`, and `rejected`; it raises `RuleError` for `approved`, because approved entries are immutable.\n- Unit tests use pytest.\n\n## Test requirements\n- Conformance tests use pytest.\n- Conformance tests call only the public API.\n- Include the exact 24-hour day boundary, a zero-hours rejection, and illegal transitions from every status.\n\n## Stack adapter guidance\n- Write the implementation under src/ and generated unit tests under tests/ named test_*.py (pytest discovery); every functional unit must ship at least one unit test.\n- Write the conformance test for the CURRENT unit only, with root 'conformance', at the path FRn/... (for example FR1/test_fr1.py). Do not add a tests/ or module-name prefix, and do not create or modify earlier units' conformance tests.\n- Do not write outside the generated module patch root or conformance patch root.\n- Grow the public API incrementally for the current unit and prior rendered units; do not create placeholder stubs for future functional units.\n- Declare third-party dependencies in the generated pyproject.toml [project] dependencies; Mint installs them before tests run.\n\n## Units already rendered\n- FR1: Validate hours against the daily cap\n\n## Implement unit FR1: Validate hours against the daily cap\nSpec:\n- `validate_hours(hours, day_total=0.0)` returns None when `hours > 0` and `day_total + hours <= 24`.\n- It raises `RuleError` at call time when `hours <= 0` or when `day_total + hours` exceeds 24.\nAcceptance:\n- `validate_hours(8.0)` returns None.\n- `validate_hours(10.0, day_total=14.0)` returns None \u2014 the new day total equals the exact 24-hour boundary and is accepted.\n- `validate_hours(10.5, day_total=14.0)` raises `RuleError` whose message contains `24`.\n- `validate_hours(0)` raises `RuleError` whose message contains `positive`.\n- `validate_hours(-1.5)` raises `RuleError`.\n\nPhase: unit (attempt 1)\n\nReturn the JSON patch now.",
  "promptVersion": "timesheet-v1",
  "request": {
    "attempt": 1,
    "module": "rules",
    "phase": "unit",
    "promptHash": "523950b6c33fedaca931ef0ef0717f17973fb70bbd5e1ddab69f385c488cab1e",
    "unit": "FR1"
  },
  "response": "```json\n{\"summary\": \"Implement RuleError and validate_hours with unit and conformance tests for FR1\", \"files\": [{\"path\": \"src/rules/__init__.py\", \"action\": \"write\", \"root\": \"module\", \"contents\": \"\\\"\\\"\\\"Pure business rules for timesheet entries.\\\"\\\"\\\"\\n\\nfrom __future__ import annotations\\n\\nMAX_DAY_HOURS = 24.0\\n\\n\\nclass RuleError(Exception):\\n    \\\"\\\"\\\"Raised when a value is invalid or a status transition is illegal.\\\"\\\"\\\"\\n\\n\\ndef validate_hours(hours: float, day_total: float = 0.0) -> None:\\n    \\\"\\\"\\\"Validate that ``hours`` is a positive addition that keeps the day within 24 hours.\\\"\\\"\\\"\\n    if hours <= 0:\\n        raise RuleError(\\\"hours must be positive\\\")\\n    new_total = day_total + hours\\n    if new_total > MAX_DAY_HOURS:\\n        raise RuleError(\\n            f\\\"day total {new_total} would exceed the 24-hour day limit\\\"\\n        )\\n    return None\\n\"}, {\"path\": \"tests/test_rules.py\", \"action\": \"write\", \"root\": \"module\", \"contents\": \"import pytest\\n\\nfrom rules import RuleError, validate_hours\\n\\n\\ndef test_validate_hours_accepts_simple_positive_value():\\n    assert validate_hours(8.0) is None\\n\\n\\ndef test_validate_hours_accepts_exact_24_hour_boundary():\\n    assert validate_hours(10.0, day_total=14.0) is None\\n\\n\\ndef test_validate_hours_rejects_over_24_hour_boundary():\\n    with pytest.raises(RuleError, match=\\\"24\\\"):\\n        validate_hours(10.5, day_total=14.0)\\n\\n\\ndef test_validate_hours_rejects_zero_hours():\\n    with pytest.raises(RuleError, match=\\\"positive\\\"):\\n        validate_hours(0)\\n\\n\\ndef test_validate_hours_rejects_negative_hours():\\n    with pytest.raises(RuleError, match=\\\"positive\\\"):\\n        validate_hours(-1.5)\\n\\n\\ndef test_validate_hours_defaults_day_total_to_zero():\\n    assert validate_hours(24.0) is None\\n\\n\\ndef test_validate_hours_rejects_just_over_boundary_with_default_day_total():\\n    with pytest.raises(RuleError, match=\\\"24\\\"):\\n        validate_hours(24.1)\\n\"}, {\"path\": \"FR1/test_fr1.py\", \"action\": \"write\", \"root\": \"conformance\", \"contents\": \"import pytest\\n\\nfrom rules import RuleError, validate_hours\\n\\n\\ndef test_validate_hours_returns_none_for_simple_positive_value():\\n    assert validate_hours(8.0) is None\\n\\n\\ndef test_validate_hours_accepts_exact_24_hour_day_boundary():\\n    assert validate_hours(10.0, day_total=14.0) is None\\n\\n\\ndef test_validate_hours_raises_when_day_total_exceeds_24():\\n    with pytest.raises(RuleError, match=\\\"24\\\"):\\n        validate_hours(10.5, day_total=14.0)\\n\\n\\ndef test_validate_hours_raises_for_zero_hours():\\n    with pytest.raises(RuleError, match=\\\"positive\\\"):\\n        validate_hours(0)\\n\\n\\ndef test_validate_hours_raises_for_negative_hours():\\n    with pytest.raises(RuleError):\\n        validate_hours(-1.5)\\n\"}]}\n```",
  "system": "You are a code renderer in a regenerative coding system. You implement exactly one functional unit. Respond with ONLY a single JSON object matching this schema:\n{\"summary\": \"<short>\", \"files\": [{\"path\": \"<rel path>\", \"action\": \"write|delete\", \"contents\": \"<file text, for write>\", \"root\": \"module|conformance\"}]}\nDo not include prose, markdown fences, or explanations outside the JSON."
}
