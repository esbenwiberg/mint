{
  "cassetteVersion": 1,
  "createdAt": "2026-07-10T06:44:26Z",
  "id": "4fb6957b4774bb8ac1c72a77872174ce1a38e0d424ce4922b2fcb9b31b470bdf",
  "model": "claude-cli:sonnet",
  "prompt": "# Render request (prompt timesheet-v1)\nModule: timesheet-api\nStack: python-lib\n\n## Definitions\n- App factory: `create_app(store_path)` returning a FastAPI application bound to one store file.\n- Entry body: a JSON object with keys `project`, `date`, `hours`.\n- Week query: an ISO week label `YYYY-Www` passed as the `week` query parameter.\n- Error contract: 422 for an invalid body or rule violation, 404 for an unknown entry or project, 409 for an illegal transition or an edit of an approved entry.\n\n## Implementation requirements\n- Use Python 3.12.\n- Expose `create_app(store_path)` from `src/timesheet_api/`.\n- Declare `fastapi` and `httpx` in the generated `pyproject.toml` `[project]` dependencies.\n- Persist entries only through the required timestore module and enforce every rule only through the required rules module; the app holds no state of its own.\n- POST `/entries` validates the body, checks `validate_hours` against the total hours already stored for the body's date read from the store, and stores a new `draft` entry.\n- GET `/projects/{project}/entries` returns the project's entries, filtered to the `week` query parameter when one is given; a malformed week label responds 422.\n- POST `/entries/{entry_id}/submit`, `/approve`, and `/reject` move the entry through the rules state machine and persist the new status.\n- PUT `/entries/{entry_id}` replaces `project`, `date`, and `hours` on an entry after `assert_editable` and `validate_hours` pass; the entry keeps its status.\n- Map errors at request time: an invalid body shape, date, or rule violation responds 422; an unknown entry id or unknown project responds 404; a `RuleError` from a status transition or from editing an approved entry responds 409. Every error response carries a JSON `detail` field.\n- A project is unknown when the store holds no entries for it at all; a known project with no entries in the requested week responds 200 with an empty list.\n- Unit tests use pytest.\n\n## Test requirements\n- Conformance tests use pytest and drive the app in-process with `fastapi.testclient.TestClient`; never start a server subprocess.\n- Conformance tests point each app at a `tmp_path` store file via `create_app(store_path)`.\n- Include every status code in the error contract: 201, 200, 422, 404, and 409.\n\n## Stack adapter guidance\n- Write the implementation under src/ and generated unit tests under tests/ named test_*.py (pytest discovery); every functional unit must ship at least one unit test.\n- Write the conformance test for the CURRENT unit only, with root 'conformance', at the path FRn/... (for example FR1/test_fr1.py). Do not add a tests/ or module-name prefix, and do not create or modify earlier units' conformance tests.\n- Do not write outside the generated module patch root or conformance patch root.\n- Grow the public API incrementally for the current unit and prior rendered units; do not create placeholder stubs for future functional units.\n- Declare third-party dependencies in the generated pyproject.toml [project] dependencies; Mint installs them before tests run.\n- Required modules (timestore, rules) are shown as public interface stubs and are importable by package name at test time; do not pip-depend on them, and call them only through that public interface.\n\n## Imported context\n### from timestore\n- def Entry: a plain dict with keys `id`, `project`, `date`, `hours`, `status`.\n- def Store file: a JSON file holding every entry; its path comes from an explicit constructor argument or the `TIMESHEET_STORE` environment variable.\n- def ISO week: a week label written `YYYY-Www` with a zero-padded week number, for example `2026-W28`.\n- def StoreError: the typed error for store misuse \u2014 a missing store path, an invalid date, an invalid week label, or an unknown entry id.\n### from rules\n- def Status: one of `draft`, `submitted`, `approved`, `rejected`.\n- def Day total: the sum of hours a person already has registered on one calendar date.\n- def RuleError: the typed error raised for an invalid value or an illegal status transition.\n\n## Required modules (already generated)\n### timestore\n#### src/timestore/__init__.py\n```python\n\"\"\"Timesheet entry store backed by a JSON file.\"\"\"\n\nfrom __future__ import annotations\n\nimport json\n\nimport os\n\nimport re\n\nfrom datetime import date as _date\n\nfrom pathlib import Path\n\nfrom typing import Any\n\nclass StoreError(Exception):\n    \"\"\"Raised for store misuse: missing path, invalid date/week, or unknown entry id.\"\"\"\n\nclass TimeStore:\n\n    def __init__(self, path: str | os.PathLike[str] | None=None) -> None:\n        ...\n\n    def add_entry(self, project: str, date: str, hours: float) -> dict[str, Any]:\n        ...\n\n    def get_entry(self, entry_id: int) -> dict[str, Any]:\n        ...\n\n    def update_entry(self, entry_id: int, **fields: Any) -> dict[str, Any]:\n        ...\n\n    def delete_entry(self, entry_id: int) -> None:\n        ...\n\n    def list_entries(self) -> list[dict[str, Any]]:\n        ...\n\n    def entries_for_project(self, project: str) -> list[dict[str, Any]]:\n        ...\n\n    def entries_for_date(self, date: str) -> list[dict[str, Any]]:\n        ...\n\n    def entries_for_week(self, week: str) -> list[dict[str, Any]]:\n        ...\n\n```\n### rules\n#### src/rules/__init__.py\n```python\n\"\"\"Pure business rules for timesheet entries.\"\"\"\n\nfrom __future__ import annotations\n\nMAX_DAY_HOURS = 24.0\n\nVALID_STATUSES = {'draft', 'submitted', 'approved', 'rejected'}\n\nLEGAL_TRANSITIONS = {('draft', 'submitted'): 'submitted', ('submitted', 'approved'): 'approved', ('submitted', 'rejected'): 'rejected'}\n\nclass RuleError(Exception):\n    \"\"\"Raised when a value is invalid or a status transition is illegal.\"\"\"\n\ndef validate_hours(hours: float, day_total: float=0.0) -> None:\n    \"\"\"Validate that ``hours`` is a positive addition that keeps the day within 24 hours.\"\"\"\n    ...\n\ndef transition(current: str, target: str) -> str:\n    \"\"\"Return ``target`` if moving from ``current`` to ``target`` is a legal status change.\"\"\"\n    ...\n\ndef assert_editable(status: str) -> None:\n    \"\"\"Raise ``RuleError`` if entries in ``status`` can no longer be edited.\"\"\"\n    ...\n\n```\n\n## Current module files (already rendered for earlier units)\nExtend and stay consistent with this code; reuse its exact public names.\n#### src/timesheet_api/__init__.py\n```python\n\"\"\"Timesheet API FastAPI application.\"\"\"\n\nfrom __future__ import annotations\n\nimport os\nfrom typing import Any\n\nfrom fastapi import FastAPI, HTTPException\nfrom pydantic import BaseModel\n\nfrom rules import RuleError, validate_hours\nfrom timestore import StoreError, TimeStore\n\n\nclass EntryIn(BaseModel):\n    project: str\n    date: str\n    hours: float\n\n\ndef create_app(store_path: str | os.PathLike[str] | None = None) -> FastAPI:\n    app = FastAPI()\n    store = TimeStore(store_path)\n\n    @app.post(\"/entries\", status_code=201)\n    def create_entry(body: EntryIn) -> Any:\n        try:\n            existing = store.entries_for_date(body.date)\n        except StoreError as exc:\n            raise HTTPException(status_code=422, detail=str(exc)) from exc\n\n        day_total = sum(entry[\"hours\"] for entry in existing)\n\n        try:\n            validate_hours(body.hours, day_total)\n        except RuleError as exc:\n            raise HTTPException(status_code=422, detail=str(exc)) from exc\n\n        try:\n            entry = store.add_entry(body.project, body.date, body.hours)\n        except StoreError as exc:\n            raise HTTPException(status_code=422, detail=str(exc)) from exc\n\n        return entry\n\n    @app.get(\"/projects/{project}/entries\")\n    def list_project_entries(project: str, week: str | None = None) -> Any:\n        entries = store.entries_for_project(project)\n        if not entries:\n            raise HTTPException(\n                status_code=404, detail=f\"unknown project: {project}\"\n            )\n\n        if week is not None:\n            try:\n                week_entries = store.entries_for_week(week)\n            except StoreError as exc:\n                raise HTTPException(status_code=422, detail=str(exc)) from exc\n\n            week_ids = {entry[\"id\"] for entry in week_entries}\n            entries = [entry for entry in entries if entry[\"id\"] in week_ids]\n\n        return entries\n\n    return app\n\n```\n\n## Units already rendered\n- FR1: Create entries with validation\n- FR2: Query a project's entries by ISO week\n- FR3: Submit, approve, and reject entries\n\n## Implement unit FR3: Submit, approve, and reject entries\nSpec:\n- POST `/entries/{entry_id}/submit` moves `draft` to `submitted`; `/approve` and `/reject` move `submitted` to `approved` and `rejected`; each responds 200 with the updated entry.\n- An unknown entry id responds 404.\n- An illegal transition responds 409 and leaves the stored status unmodified.\nAcceptance:\n- After creating a draft entry with id 1, POST `/entries/1/submit` responds 200 and the body contains `\"status\": \"submitted\"`.\n- After that submit, POST `/entries/1/approve` responds 200 and the body contains `\"status\": \"approved\"`.\n- POST `/entries/999/submit` responds 404.\n- POST `/entries/1/approve` while entry 1 is still `draft` responds 409, and a following GET shows the entry still `draft`.\n\nPhase: unit (attempt 1)\n\nReturn the JSON patch now.",
  "promptVersion": "timesheet-v1",
  "request": {
    "attempt": 1,
    "module": "timesheet-api",
    "phase": "unit",
    "promptHash": "8d6c9f00accc8d7673f7c6c5d43c2014c173a59f6ebd935725b1e85860436c9e",
    "unit": "FR3"
  },
  "response": "```json\n{\"summary\": \"Add submit/approve/reject endpoints moving entries through the rules state machine (200/404/409), with unit and conformance tests.\", \"files\": [{\"path\": \"src/timesheet_api/__init__.py\", \"action\": \"write\", \"root\": \"module\", \"contents\": \"\\\"\\\"\\\"Timesheet API FastAPI application.\\\"\\\"\\\"\\n\\nfrom __future__ import annotations\\n\\nimport os\\nfrom typing import Any\\n\\nfrom fastapi import FastAPI, HTTPException\\nfrom pydantic import BaseModel\\n\\nfrom rules import RuleError, transition, validate_hours\\nfrom timestore import StoreError, TimeStore\\n\\n\\nclass EntryIn(BaseModel):\\n    project: str\\n    date: str\\n    hours: float\\n\\n\\ndef create_app(store_path: str | os.PathLike[str] | None = None) -> FastAPI:\\n    app = FastAPI()\\n    store = TimeStore(store_path)\\n\\n    def _advance(entry_id: int, target: str) -> Any:\\n        try:\\n            entry = store.get_entry(entry_id)\\n        except StoreError as exc:\\n            raise HTTPException(status_code=404, detail=str(exc)) from exc\\n\\n        try:\\n            new_status = transition(entry[\\\"status\\\"], target)\\n        except RuleError as exc:\\n            raise HTTPException(status_code=409, detail=str(exc)) from exc\\n\\n        return store.update_entry(entry_id, status=new_status)\\n\\n    @app.post(\\\"/entries\\\", status_code=201)\\n    def create_entry(body: EntryIn) -> Any:\\n        try:\\n            existing = store.entries_for_date(body.date)\\n        except StoreError as exc:\\n            raise HTTPException(status_code=422, detail=str(exc)) from exc\\n\\n        day_total = sum(entry[\\\"hours\\\"] for entry in existing)\\n\\n        try:\\n            validate_hours(body.hours, day_total)\\n        except RuleError as exc:\\n            raise HTTPException(status_code=422, detail=str(exc)) from exc\\n\\n        try:\\n            entry = store.add_entry(body.project, body.date, body.hours)\\n        except StoreError as exc:\\n            raise HTTPException(status_code=422, detail=str(exc)) from exc\\n\\n        return entry\\n\\n    @app.get(\\\"/projects/{project}/entries\\\")\\n    def list_project_entries(project: str, week: str | None = None) -> Any:\\n        entries = store.entries_for_project(project)\\n        if not entries:\\n            raise HTTPException(\\n                status_code=404, detail=f\\\"unknown project: {project}\\\"\\n            )\\n\\n        if week is not None:\\n            try:\\n                week_entries = store.entries_for_week(week)\\n            except StoreError as exc:\\n                raise HTTPException(status_code=422, detail=str(exc)) from exc\\n\\n            week_ids = {entry[\\\"id\\\"] for entry in week_entries}\\n            entries = [entry for entry in entries if entry[\\\"id\\\"] in week_ids]\\n\\n        return entries\\n\\n    @app.post(\\\"/entries/{entry_id}/submit\\\")\\n    def submit_entry(entry_id: int) -> Any:\\n        return _advance(entry_id, \\\"submitted\\\")\\n\\n    @app.post(\\\"/entries/{entry_id}/approve\\\")\\n    def approve_entry(entry_id: int) -> Any:\\n        return _advance(entry_id, \\\"approved\\\")\\n\\n    @app.post(\\\"/entries/{entry_id}/reject\\\")\\n    def reject_entry(entry_id: int) -> Any:\\n        return _advance(entry_id, \\\"rejected\\\")\\n\\n    return app\\n\"}, {\"path\": \"tests/test_fr3.py\", \"action\": \"write\", \"root\": \"module\", \"contents\": \"from __future__ import annotations\\n\\nfrom fastapi.testclient import TestClient\\n\\nfrom timesheet_api import create_app\\n\\n\\ndef _make_client(tmp_path):\\n    store_path = tmp_path / \\\"store.json\\\"\\n    app = create_app(store_path)\\n    return TestClient(app)\\n\\n\\ndef test_submit_moves_draft_to_submitted(tmp_path):\\n    client = _make_client(tmp_path)\\n    created = client.post(\\n        \\\"/entries\\\", json={\\\"project\\\": \\\"acme\\\", \\\"date\\\": \\\"2026-07-06\\\", \\\"hours\\\": 4}\\n    ).json()\\n\\n    response = client.post(f\\\"/entries/{created['id']}/submit\\\")\\n\\n    assert response.status_code == 200\\n    assert response.json()[\\\"status\\\"] == \\\"submitted\\\"\\n\\n\\ndef test_submit_then_approve(tmp_path):\\n    client = _make_client(tmp_path)\\n    created = client.post(\\n        \\\"/entries\\\", json={\\\"project\\\": \\\"acme\\\", \\\"date\\\": \\\"2026-07-06\\\", \\\"hours\\\": 4}\\n    ).json()\\n    client.post(f\\\"/entries/{created['id']}/submit\\\")\\n\\n    response = client.post(f\\\"/entries/{created['id']}/approve\\\")\\n\\n    assert response.status_code == 200\\n    assert response.json()[\\\"status\\\"] == \\\"approved\\\"\\n\\n\\ndef test_submit_then_reject(tmp_path):\\n    client = _make_client(tmp_path)\\n    created = client.post(\\n        \\\"/entries\\\", json={\\\"project\\\": \\\"acme\\\", \\\"date\\\": \\\"2026-07-06\\\", \\\"hours\\\": 4}\\n    ).json()\\n    client.post(f\\\"/entries/{created['id']}/submit\\\")\\n\\n    response = client.post(f\\\"/entries/{created['id']}/reject\\\")\\n\\n    assert response.status_code == 200\\n    assert response.json()[\\\"status\\\"] == \\\"rejected\\\"\\n\\n\\ndef test_unknown_entry_id_returns_404(tmp_path):\\n    client = _make_client(tmp_path)\\n\\n    response = client.post(\\\"/entries/999/submit\\\")\\n\\n    assert response.status_code == 404\\n    assert \\\"detail\\\" in response.json()\\n\\n\\ndef test_illegal_transition_returns_409_and_leaves_status(tmp_path):\\n    client = _make_client(tmp_path)\\n    created = client.post(\\n        \\\"/entries\\\", json={\\\"project\\\": \\\"acme\\\", \\\"date\\\": \\\"2026-07-06\\\", \\\"hours\\\": 4}\\n    ).json()\\n\\n    response = client.post(f\\\"/entries/{created['id']}/approve\\\")\\n\\n    assert response.status_code == 409\\n    assert \\\"detail\\\" in response.json()\\n\\n    listed = client.get(\\\"/projects/acme/entries\\\").json()\\n    assert listed[0][\\\"status\\\"] == \\\"draft\\\"\\n\"}, {\"path\": \"FR3/test_fr3.py\", \"action\": \"write\", \"root\": \"conformance\", \"contents\": \"from __future__ import annotations\\n\\nfrom fastapi.testclient import TestClient\\n\\nfrom timesheet_api import create_app\\n\\n\\ndef _make_client(tmp_path):\\n    store_path = tmp_path / \\\"store.json\\\"\\n    app = create_app(store_path)\\n    return TestClient(app)\\n\\n\\ndef test_submit_responds_200_with_submitted_status(tmp_path):\\n    client = _make_client(tmp_path)\\n    created = client.post(\\n        \\\"/entries\\\", json={\\\"project\\\": \\\"acme\\\", \\\"date\\\": \\\"2026-07-06\\\", \\\"hours\\\": 4}\\n    ).json()\\n    assert created[\\\"id\\\"] == 1\\n\\n    response = client.post(\\\"/entries/1/submit\\\")\\n\\n    assert response.status_code == 200\\n    assert response.json()[\\\"status\\\"] == \\\"submitted\\\"\\n\\n\\ndef test_approve_after_submit_responds_200_with_approved_status(tmp_path):\\n    client = _make_client(tmp_path)\\n    client.post(\\\"/entries\\\", json={\\\"project\\\": \\\"acme\\\", \\\"date\\\": \\\"2026-07-06\\\", \\\"hours\\\": 4})\\n    client.post(\\\"/entries/1/submit\\\")\\n\\n    response = client.post(\\\"/entries/1/approve\\\")\\n\\n    assert response.status_code == 200\\n    assert response.json()[\\\"status\\\"] == \\\"approved\\\"\\n\\n\\ndef test_submit_unknown_entry_responds_404(tmp_path):\\n    client = _make_client(tmp_path)\\n\\n    response = client.post(\\\"/entries/999/submit\\\")\\n\\n    assert response.status_code == 404\\n    assert \\\"detail\\\" in response.json()\\n\\n\\ndef test_approve_draft_entry_responds_409_and_status_unchanged(tmp_path):\\n    client = _make_client(tmp_path)\\n    client.post(\\\"/entries\\\", json={\\\"project\\\": \\\"acme\\\", \\\"date\\\": \\\"2026-07-06\\\", \\\"hours\\\": 4})\\n\\n    response = client.post(\\\"/entries/1/approve\\\")\\n\\n    assert response.status_code == 409\\n    assert \\\"detail\\\" in response.json()\\n\\n    listed = client.get(\\\"/projects/acme/entries\\\").json()\\n    assert listed[0][\\\"status\\\"] == \\\"draft\\\"\\n\"}]}\n```",
  "system": "You are a code renderer in a regenerative coding system. You implement exactly one functional unit. Respond with ONLY a single JSON object matching this schema:\n{\"summary\": \"<short>\", \"files\": [{\"path\": \"<rel path>\", \"action\": \"write|delete\", \"contents\": \"<file text, for write>\", \"root\": \"module|conformance\"}]}\nDo not include prose, markdown fences, or explanations outside the JSON."
}
